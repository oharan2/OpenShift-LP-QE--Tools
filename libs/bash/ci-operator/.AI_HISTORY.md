# AI History Log
This file tracks important changes and decisions in this directory and its sub-directories (when sensible).

## 2026-05-27
### ExitTrap jUnit merge: drop placeholder testsuites
- `ExitTrap--PostProcessPrep.sh` yq merge now filters non-map `testsuite` entries (e.g. literal `null`) and removes empty or placeholder nested `<testsuite>` nodes after flattening.
- Prevents merged XML from containing `<testsuite>null</testsuite>` from stray or malformed inputs under `ARTIFACT_DIR`.

## 2026-03-10
### Common Bash Library Additions
- Moved `PreAction--EnsureReqs` to `libs/bash/common/EnsureReqs` (renamed, generalized from CI Operator context).
- Created `libs/bash/common/BuildCustomScriptsFromYAML` (builds compounded sub-shell scripts from YAML input).

## 2026-02-13
### Initial Setup
- Created `.AI_README.md` with local conventions.
- Created `common/PreAction--EnsureReqs.sh` (tool installer for `jq`/`yq`).
- Created `interop/common/ExitTrap--PostProcessPrep.sh` (EXIT trap for jUnit XML post-processing).
